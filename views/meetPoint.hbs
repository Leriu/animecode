<h1>{{name}}</h1>
<p>{{location.address}}</p>

<div id="map"></div>

<script src=""></script>
<script>
   let center = {
       lat:{{location.coordinates.[index]}},
       lng:{{location.coordinates.[index]}}
   };
   let title = '{{name}}'

   const map = new google.maps.Map(
       document.getElementById('map'),
       {
           center,
           zoom:
       }
   )
   const marker = new google.maps.Marker({
       map,
       position:center,
       title,
       animation: google.maps.Animation.BOUNCE
   })

</script>